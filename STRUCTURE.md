# Структура проекта — Конверкот

**Версия:** 0.18.0 | **Дата:** 17.09.2026

---

```
konvert-me/
├── .gitignore                    # Игнорируемые файлы
├── .prettierignore               # Файлы вне форматирования (в т.ч. CLAUDE.md, docs/PLAN.md и клон публикации .deploy)
├── .prettierrc.json              # Настройки Prettier
├── CHANGELOG.md                  # История изменений по версиям
├── CLAUDE.md                     # Правила работы над проектом (стек, жёсткие правила, порядок работы по этапам)
├── README.md                     # Краткое описание, состояние этапов, команды
├── STACK.md                      # Стек и зависимости
├── STRUCTURE.md                  # Этот файл
├── eslint.config.js              # ESLint: общие правила + запрет зависимостей ядра от React/базы/экранов
├── index.html                    # Точка входа, мета-теги и превью ссылки, иконки, тема до первой отрисовки
├── package.json                  # Зависимости и команды (здесь же версия приложения)
├── package-lock.json             # Lockfile
├── playwright.config.ts          # E2E: desktop 1280px и mobile 375px, после них — доступность, затем скорость; сборка + preview
├── tsconfig.json                 # Ссылки на конфигурации
├── tsconfig.app.json             # TypeScript для приложения (strict, alias @/*)
├── tsconfig.node.json            # TypeScript для конфигов и e2e
├── vite.config.ts                # Vite: React, Tailwind, PWA-манифест (id и старт на «Обзоре»), версия и адрес сайта в сборку
├── vitest.config.ts              # Vitest: jsdom, версия приложения, пороги покрытия (ядро — 95 %)
│
├── docs/
│   ├── PLAN.md                   # Полное ТЗ: принципы, модель данных, формулы (раздел 5 — источник истины), этапы
│   ├── PILOT.md                  # Пилот: метрики и ориентиры, почему GitVerse Pages, публикация, анкета, календарь
│   ├── GLOSSARY.md               # Термины на русском и английском, стиль английских текстов
│   └── assets/
│       ├── overview.png          # Скриншот «Обзора» для README
│       └── plan.png              # Скриншот пенсионного калькулятора для README
│
├── .claude/
│   └── launch.json               # Конфигурация запуска дев-сервера
│
├── .github/
│   └── workflows/
│       └── ci.yml                # CI: линт → типы → тесты с покрытием → сборка → e2e
│
├── scripts/
│   ├── generate-icons.mjs        # Иконки из cat-mark.ts браузером Playwright: PWA, крупный значок вкладки (favicon.svg по теме, favicon.ico 16/32/48) (npm run icons)
│   ├── generate-og-image.mjs     # Картинка превью ссылки 1200×630 с котом (npm run og-image)
│   └── deploy.mjs                # Публикация на GitVerse Pages: сборка из коммита, 404.html и копии по адресам, push (npm run deploy)
│
├── public/                       # ===== СТАТИКА =====
│   ├── favicon.svg               # Знак, меняющий цвет по системной теме (у фигуры один атрибут class: SVG читается как XML)
│   ├── favicon.ico               # 16, 32 и 48 px: закладки, плитки и вкладки, которые просят его по имени
│   ├── favicon-64.png            # Растровый favicon: плитка приложения, видна на светлой и тёмной полосе вкладок
│   ├── apple-touch-icon.png      # Иконка для iOS
│   ├── pwa-192x192.png           # Иконка установленного приложения
│   ├── pwa-512x512.png           # Иконка установленного приложения
│   ├── pwa-maskable-512x512.png  # Maskable-иконка (обрезается системой по маске)
│   ├── og-image.png              # Превью ссылки в мессенджерах (не в офлайн-кэше)
│   └── landing/
│       ├── overview.png          # Снимок «Обзора» для лендинга (не в офлайн-кэше)
│       └── plan.png              # Снимок калькулятора пенсии для лендинга
│
├── e2e/                          # ===== E2E-ТЕСТЫ (Playwright) =====
│   ├── helpers.ts                # skipOnboarding() — общий пролог для тестов не про знакомство
│   ├── backup-seed.ts            # Месяцы хозяйства через резервную копию: seed(), счёт, операция, цель
│   ├── a11y.ts                   # Проверки доступности: контраст по WCAG, подписи из дерева доступности, обход по Tab
│   ├── navigation.spec.ts        # Маршруты, вкладки сверху и столбиком на телефоне (правша и левша), тема, без горизонтальной прокрутки на 375 px
│   ├── pwa.spec.ts               # Манифест, работа без интернета, значок вкладки, version.json и «Конверкот обновлён» один раз
│   ├── site.spec.ts              # Лендинг, политика отовсюду, статистика пилота, напоминание на 7-й день, письмо о проблеме
│   ├── storage.spec.ts           # Счета, источники дохода, копия → очистка → импорт, риски потери данных, две вкладки
│   ├── onboarding.spec.ts        # Пять шагов ≤ 25 действий, дашборд, добавление и перестановка виджетов, быстрый ввод
│   ├── sources.spec.ts           # Закладки, новости, ключи API, отказ источника
│   ├── budget.spec.ts            # Операции всех видов, план-факт, копирование плана, поиск и фильтры, год
│   ├── goals.spec.ts             # Векторы плана через интерфейс, взнос и конверты, «что если», распределение
│   ├── balance.spec.ts           # Капитал и кредитная нагрузка, график, лимит вкладов, полисы, «Скоро»
│   ├── import.spec.ts            # Выписка в Windows-1251, повторный импорт, правила, дубли, отмена
│   ├── deductions.spec.ts        # Примеры ФНС (обучение, дети, продажа), жильё, чек-лист, напоминание, виджет, документы
│   ├── plan.spec.ts              # Финплан: шаги, векторы образования и пенсии через интерфейс, риск, действия, пересмотр, PDF
│   ├── scenarios.spec.ts         # Регрессии проверки на двух людях: обычный месяц 16-го и 30-го, долг и резерв раньше целей, ставка долга, конверт на брокерском и на квартире, резерв новичка
│   ├── card-scenarios.spec.ts    # Регрессии «Рабочий и кредитка Платинум»: 13 и 15 октября, перевод с карты, условия карты, проценты, категории
│   ├── language.spec.ts          # Русский без сохранённого языка; английский без кириллицы на лендинге, политике, в знакомстве, разделах и шагах финплана
│   ├── country.spec.ts           # Страна скрыта до 9.5; знакомство и суммы в долларах, смена страны туда и обратно без пересчёта, копия другой страны (и зашифрованная) с предупреждением
│   ├── accessibility.spec.ts     # Лендинг, политика, все экраны, диалоги и знакомство в двух темах: контраст, клавиатура, подписи
│   └── performance.spec.ts       # 10 000 операций через копию: «Бюджет» открывается и «Обзор» пересчитывается ≤ 500 мс
│
└── src/                          # ===== ИСХОДНЫЙ КОД =====
    ├── main.tsx                  # Точка входа React (ThemeProvider + BrowserRouter)
    ├── build-info.d.ts           # __APP_VERSION__: версия из package.json, подставляет сборка
    ├── index.css                 # Tailwind, чёрно-белая палитра в CSS-переменных, плавный переход темы
    │
    ├── app/                      # Оболочка приложения
    │   ├── App.tsx               # Маршруты: сайт (/ и /privacy), знакомство, вкладки внутри оболочки
    │   ├── app-session.tsx       # Только приложение: считает день использования и просит постоянное хранение; ждёт настроек, ставит страну данных и рисует экраны заново при её смене
    │   ├── country-route.tsx     # Раздел только некоторых стран: из других адрес ведёт на «Обзор»
    │   ├── app-shell.tsx         # Вкладки сверху, на телефоне столбиком у края руки; жук и тема в шапке; плашки: обновление, риск данных, кредитка, копия, декларация, пересмотр, пилот
    │   ├── release-notes.json    # Что нового в каждой версии: оповещение об обновлении и version.json сборки
    │   ├── site.json             # Адрес сайта, репозиторий публикации, анкета, почта, выключатель пилота, адреса приложения
    │   ├── site.ts               # Типы и описание настроек сайта
    │   ├── site.test.ts          # Адреса совпадают с вкладками, https в корне, репозиторий назван как владелец
    │   ├── navigation.ts         # Список вкладок: адрес, подпись, значок, страны; navItemsFor(страна)
    │   └── navigation.test.ts    # Вкладки и виджеты страны: «Вычеты» только у России
    │
    ├── core/                     # ===== ФИНАНСОВОЕ ЯДРО =====
    │   │                         # Чистые функции: без React, без базы, без экранов (правило проверяется ESLint)
    │   ├── money.ts              # Копейки: проверки, округление half-up, формат по языку и валюте страны, знак валюты, вывод прогноза, короткая шкала графиков (250 млн)
    │   ├── country.ts            # Страны (Россия, США), их валюты (RUB, USD), страна по умолчанию
    │   ├── time.ts               # Даты строками ГГГГ-ММ-ДД и месяцы ГГГГ-ММ, разница месяцев, без часовых поясов
    │   ├── goals.ts              # Формулы 2-6 и 8: будущая стоимость, взнос, срок перебором 1..1200, реальная доходность, накопления на деньгах, распределение остатка
    │   ├── budget.ts             # Формула 7: доходы минус расходы, агрегаты месяца и года, план против факта, обычный месяц для выводов
    │   ├── balance.ts            # Формулы 9-11: резерв, минимальный платёж от долга сегодня, основной долг по графику, взнос в резерв; динамика капитала, вклады по банкам
    │   ├── payday.ts             # Ближайшая выплата по числу месяца и сколько до неё дней
    │   ├── upcoming.ts           # Лента «Скоро»: платежи по долгу, вся выписка кредитки к сроку, вклады, полисы, льготный период
    │   ├── credit-card.ts        # Льготный период по условиям договора: выписка к оплате, внесена, пропущена с процентами, один длинный период
    │   ├── interest.ts           # Проценты накопительного счёта за месяц по остатку каждого дня
    │   ├── deductions.ts         # НДФЛ по шкале, возврат, вычеты: дети, социальный, ИИС, жильё с остатком по стоимости и процентам; продажа жилья и итог года
    │   ├── pension.ts            # Формула 12: потребность на пенсии, капитал на доход и на срок, упрощённый A / r, капитал по годам
    │   ├── education.ts          # Образование: одной суммой к началу и по годам учёбы с дисконтированием месячной ставкой
    │   ├── portfolio.ts          # Доли акций, облигаций, недвижимости и золота по сроку и профилю риска; разбивка суммы до копейки
    │   ├── financial-plan.ts     # Тип бюджета (дефицит / баланс / профицит) и сроки пересмотра: квартал и год
    │   ├── rules/                # Нормативы по странам и годам: значение, источник (URL), дата проверки
    │   │   ├── types.ts          # Norm<T>, день года, общие поля года всех стран (страхование вкладов)
    │   │   ├── index.ts          # rulesForYear(страна, год): свой год или последний записанный; раньше первого — ничего
    │   │   ├── us/               # США: types.ts, sources.ts, 2026.ts — лимит FDIC $250 000
    │   │   └── ru/               # Россия
    │   │       ├── types.ts      # Ступень шкалы НДФЛ, детские вычеты, продажа жилья, набор правил года России
    │   │       ├── sources.ts    # Адреса страниц нормативов (одна страница — на несколько лет), в т. ч. статьи 210, 228, 229 НК
    │   │       ├── 2023.ts       # Социальный лимит 120 000, обучение ребёнка 50 000, НДФЛ 13/15 %, ИИС типа А, продажа по 13 %
    │   │       ├── 2024.ts       # Социальный лимит 150 000, обучение ребёнка 110 000, ПДС и ИИС-3 — общий лимит 400 000
    │   │       ├── 2025.ts       # Пять ступеней НДФЛ, детские 1 400 / 2 800 / 6 000, продажа — отдельная база 13/15 %
    │   │       └── 2026.ts       # То же + лимит страхования вкладов 1 400 000 ₽; страхование жизни в вычете на долгосрочные сбережения с 01.09.2026 (только подсказка)
    │   ├── types.ts              # Входные типы ядра (операция, счёт, категория, план)
    │   ├── index.ts              # Реэкспорт
    │   └── *.test.ts             # Векторы раздела 5 плана; для вычетов — примеры ФНС и правила закона, у каждого вектора подписан источник
    │
    ├── db/                       # ===== ХРАНИЛИЩЕ =====
    │   ├── db.ts                 # Dexie: таблицы, схема v1 → v8 (ленты, доходы, полисы, вычеты, финплан, жильё по частям, условия кредитки и категория подписок), versionchange
    │   ├── upgrades.ts           # Запись старой схемы → текущая: одна функция для миграции базы и импорта старой копии
    │   ├── default-categories.ts # Стартовые категории (22) и те, что добавлены после первой версии
    │   ├── models.ts             # Zod-схемы всех сущностей, значения по умолчанию, версия схемы
    │   ├── validate.ts           # parseOrThrow: ошибки Zod → одно понятное сообщение
    │   ├── errors.ts             # RepositoryError и ValidationError
    │   ├── backup.ts             # Экспорт/импорт всех таблиц с файлами документов в base64 и финпланом, копии прежних схем, шифрование, очистка базы
    │   ├── migration.test.ts     # Миграции v1 → v2, v4 → v5, v5 → v6, v6 → v7 и v7 → v8 (на настоящей цепочке версий), обновление схемы из другой вкладки
    │   └── repositories/         # Единственная дверь к данным для интерфейса
    │       ├── accounts.ts       # Счета и долги, расчётный остаток, инвариант конвертов, какие счета хранят деньги
    │       ├── transactions.ts   # Операции: правка, удаление, фильтры, поиск, пакетный импорт и его отмена
    │       ├── categories.ts     # Стартовые категории, свои без повторов в одном виде, архив
    │       ├── budget-plans.ts   # План месяца и года по категориям, копирование с прошлого месяца
    │       ├── income-sources.ts # Источники дохода и отсчёт до ближайшей выплаты
    │       ├── policies.ts       # Страховые полисы: срок, сумма, страховая компания
    │       ├── deductions.ts     # Год вычетов (ключ — год): расходы, дети, продажа, собранные бумаги; удаление с документами
    │       ├── documents.ts      # Документы: описание отдельно от байтов, лимит 10 МБ, файл по требованию
    │       ├── goals.ts          # Цели, приоритеты, цель «Резерв», конверты только на счетах с деньгами
    │       ├── dashboard.ts      # Раскладка «Обзора»: набор виджетов, ширина, высота, порядок
    │       ├── financial-plan.ts # Финплан одной строкой: расчёты образования и пенсии, риск, заметки, действия, пересмотр; расчёт как цель
    │       ├── settings.ts       # Настройки (чтение и запись одной транзакцией), дата последней копии, напоминание раз в 30 дней
    │       ├── links.ts          # Закладки (только https, никаких запросов)
    │       ├── feeds.ts          # Ленты новостей: ключи API, запрос, разбор, ошибки без секретов
    │       ├── pilot.ts          # Сколько данных каждого вида для статистики пилота: только количества
    │       └── *.test.ts         # Тесты репозиториев на fake-indexeddb
    │
    ├── features/                 # ===== ЭКРАНЫ =====
    │   ├── site/
    │   │   ├── home-page.tsx            # Главная: лендинг новому человеку, «Обзор» тому, кто уже открывал приложение
    │   │   ├── landing-page.tsx         # Лендинг: возможности, данные на устройстве, установка, пилот
    │   │   ├── privacy-page.tsx         # Политика конфиденциальности, контакт из настроек сайта
    │   │   └── site-frame.tsx           # Шапка с логотипом, темой и кнопкой, подвал с дисклеймером и версией
    │   ├── onboarding/
    │   │   ├── onboarding-page.tsx      # Пять шагов, прогресс, «Пропустить», ссылка на политику; второй раз не открывается
    │   │   ├── complete-onboarding.ts   # Ответы → категории, план, счета, цели, раскладка; как кончилось знакомство
    │   │   └── onboarding.test.ts       # Проверка результата знакомства и первых цифр «Обзора»
    │   ├── feedback/
    │   │   ├── problem-report.ts        # Письмо о проблеме: текст человека, техническая информация без данных, ссылка mailto
    │   │   ├── problem-report.test.ts   # Браузер и система, строки информации, тело и кодирование ссылки, последняя ошибка
    │   │   └── report-problem.tsx       # Значок жука в шапке и окно письма: предпросмотр, «Скопировать», «Написать письмо»
    │   ├── overview/
    │   │   ├── overview-page.tsx        # Дашборд: режим настройки, каталог, сброс
    │   │   ├── overview-data.ts         # Один расчёт на весь экран: кредитки, основной долг, лента «Скоро», предупреждения, сводка вычетов
    │   │   ├── dashboard-grid.tsx       # Сетка 4/2/1 колонки, dnd-kit, мышь, клавиатура и палец, Error Boundary на каждый виджет
    │   │   ├── widget-catalog.tsx       # Диалог «Добавить виджет»
    │   │   └── widgets/
    │   │       ├── registry.ts          # Реестр: тип, название, описание, размеры, компонент
    │   │       ├── types.ts             # Контракт виджета
    │   │       ├── widget-shell.tsx     # Рамка, пустое состояние, прогресс-бар, крупное число
    │   │       ├── basic-widgets.tsx    # Свободный остаток, резерв, капитал, кредитная нагрузка
    │   │       ├── goal-widgets.tsx     # Все цели, прогресс выбранной цели, предупреждения
    │   │       ├── quick-add-widget.tsx # Быстрый ввод операции
    │   │       ├── deductions-widget.tsx # «Вычеты: можно вернуть»: сумма открытых лет и что с декларациями
    │   │       ├── upcoming-widget.tsx  # Лента «Скоро»: ближайшие платежи, вклады, полисы, льготный период
    │   │       └── source-widgets.tsx   # Полезные источники и новости
    │   ├── balance/
    │   │   ├── balance-page.tsx         # Счета и долги, капитал, график, страхование вкладов, архив
    │   │   ├── balance-data.ts          # Один расчёт на экран: остатки, ряд капитала, банки, полисы
    │   │   ├── capital-chart.tsx        # График капитала по месяцам (Recharts, отдельный кусок)
    │   │   ├── policies-card.tsx        # Страховые полисы с отсчётом до окончания
    │   │   ├── payday-card.tsx          # Источники дохода и отсчёт до ближайшей выплаты
    │   │   ├── days-label.ts            # Склонение «день / дня / дней»
    │   │   ├── account-form.tsx         # Диалог счёта: тип, остаток на дату открытия, банк, ставка, даты, платёж; у кредитки условия договора
    │   │   ├── card-view.ts             # Даты словами, ставка, строка условий счёта, состояние льготного периода, лимит и свободно
    │   │   ├── card-reminder.ts         # Какая плашка кредитки сейчас: за неделю, за день, после срока; текст и скрытые
    │   │   ├── card-reminder-banner.tsx # Плашка кредитки над страницей
    │   │   ├── interest-card.tsx        # Проценты за прошлый месяц к записи: форма дохода заполнена заранее
    │   │   └── *.test.ts                # Строки счёта и кредитки, напоминания, выбор счетов для процентов
    │   ├── settings/
    │   │   ├── settings-page.tsx        # Сборка карточек настроек
    │   │   ├── backup-actions.ts        # Скачивание копии, чтение файла до импорта, импорт, полная очистка (и дней использования), дата последней копии
    │   │   ├── pilot-stats.ts           # Текст статистики пилота и строка для таблицы: дни и количества без сумм и имён
    │   │   ├── pilot-stats.test.ts      # Полный текст, «ещё рано» и «нет», знакомство до учёта, нет данных, нет сумм
    │   │   ├── pilot-reminder.tsx       # Плашка «Поделиться» на 7-й и 30-й день
    │   │   ├── backup-first.tsx         # «Сначала сохраните копию» в диалогах очистки, импорта и удаления года
    │   │   └── cards/
    │   │       ├── pilot-card.tsx       # Статистика пилота целиком, «Скопировать», ссылка на анкету
    │   │       ├── calculation-card.tsx # Инфляция, доходность, размер резерва
    │   │       ├── country-card.tsx     # Страна данных: предупреждение, что суммы не пересчитываются (скрыта до 9.5)
    │   │       ├── hand-card.tsx        # Вкладки на телефоне: справа для правши, слева для левши
    │   │       ├── backup-card.tsx      # Копия с паролем, импорт с подтверждением и предупреждением о другой стране, удаление данных
    │   │       ├── storage-card.tsx     # Постоянное хранение и занятое место
    │   │       ├── install-card.tsx     # Установка приложения и предупреждение про Safari
    │   │       ├── sources-card.tsx     # Выключатель внешних источников, список лент, проверка после сохранения
    │   │       └── feed-form.tsx        # Адрес ленты, способ доступа, ключ, путь к записям
    │   ├── budget/
    │   │   ├── import/
    │   │   │   ├── import-card.tsx     # Кнопка импорта и список импортов с отменой
    │   │   │   └── import-wizard.tsx   # Мастер: файл и счёт → колонки → проверка (свой чанк)
    │   │   ├── budget-page.tsx         # Вкладки «Месяц» и «Год», переключатель периода, диалоги
    │   │   ├── budget-data.ts          # Один расчёт на экран: строки план-факт, итоги месяца и года
    │   │   ├── month-label.ts          # «2026-09» → «Сентябрь 2026» и «сен», без часовых поясов
    │   │   ├── plan-fact-table.tsx     # План / факт / отклонение по категориям, план правится в строке; доход сверх плана с плюсом
    │   │   ├── plan-fact-table.test.tsx # Доход сверх плана без красного, перерасход красным
    │   │   ├── year-table.tsx          # Двенадцать месяцев и итог, под фактом серым — план
    │   │   ├── transactions-card.tsx   # Список операций месяца, поиск и фильтры
    │   │   ├── transaction-filter.ts   # Состояние фильтров списка
    │   │   ├── transaction-form.tsx    # Диалог операции: поля меняются под вид, черновик заранее, предупреждение о переводе с кредитки
    │   │   ├── card-transfer.ts        # Перевод с кредитки: в какую выписку, до какого дня, свободные деньги, бесплатные переводы
    │   │   ├── card-transfer.test.ts   # Выписка дня, тексты предупреждения
    │   │   └── categories-card.tsx     # Свои категории: добавить, в архив и обратно
    │   ├── goals/
    │   │   ├── goals-page.tsx           # Список целей с перетаскиванием, распределение свободных денег
    │   │   ├── goals-data.ts            # Один расчёт на экран: взносы, резерв, распределение
    │   │   ├── goal-form.tsx            # Диалог цели: стоимость, срок, доходность, инфляция
    │   │   ├── goal-details.tsx         # Карточка цели: цифры, график, конверты, взнос, «что если»
    │   │   ├── goal-chart.tsx           # График накоплений против цены (Recharts, отдельный кусок)
    │   │   ├── envelopes-panel.tsx      # Конверты по счетам с деньгами (старый конверт на имуществе — чтобы убрать) и форма взноса
    │   │   ├── what-if-panel.tsx        # Ползунки срока, стоимости и доходности
    │   │   └── months-label.ts          # Склонение «месяц / месяца / месяцев»
    │   ├── deductions/
    │   │   ├── deductions-page.tsx      # Годы вкладками (идёт / открыт / срок прошёл), анкета и документы выбранного года
    │   │   ├── deductions-data.ts       # Один расчёт на экран: годы, правила, итог, документы; сводка для «Обзора»
    │   │   ├── deductions-data.test.ts  # Какие годы видны, свои правила у каждого года, сводка открытых лет
    │   │   ├── year-panel.tsx           # Анкета «что мне положено»: дети, расходы, жильё, продажа; итог пересчитывается при вводе
    │   │   ├── refund-card.tsx          # Итог года: части возврата, налог с продажи, сроки декларации (и если они прошли)
    │   │   ├── refund-card.test.tsx     # Сроки до 30 апреля, между 30 апреля и 15 июля, после; продажа без налога
    │   │   ├── checklist.ts             # Какие бумаги нужны по ответам анкеты и году (урок 3.7; с 2024 — справки об оплате)
    │   │   ├── checklist.test.ts        # Пустой список, справка против договора и лицензии, дети у работодателя, ипотека, продажа
    │   │   ├── checklist-card.tsx       # Чек-лист под анкетой: группы, галочки, «собрано N из M», файлы каждого вида
    │   │   ├── reminder.ts              # О чём напомнить: январь без записей, деньги к возврату, обязательная декларация
    │   │   ├── reminder.test.ts         # Январь и после, 30 апреля, скрытое напоминание, поданный год, продажа
    │   │   ├── country.ts               # Вычеты есть только у России: вкладка, виджет, плашка и шаг финплана
    │   │   ├── deduction-reminder.tsx   # Плашка над вкладками со ссылкой на «Вычеты» и кнопкой «Скрыть»
    │   │   ├── dates.ts                 # «04-30», 2027 → «30 апреля 2027 года»
    │   │   └── documents-card.tsx       # Загрузка по видам до 10 МБ, открытие, удаление, занятое место
    │   ├── plan/
    │   │   ├── plan-page.tsx            # Восемь шагов, шаг в адресе (?step=N), кнопка PDF
    │   │   ├── plan-data.ts             # Один расчёт на экран: обзор и цели как целое, бюджет, калькуляторы, действия, пересмотр
    │   │   ├── plan-data.test.ts        # Калькуляторы, действия по данным, напоминание, весь экран
    │   │   ├── calculations.ts          # Сохранённый расчёт → что показывает карточка образования и пенсии
    │   │   ├── actions.ts               # Действия шага 7: выписка к сроку, резерв, страховка, дорогие долги, счёт и взнос цели, вычеты
    │   │   ├── debts.ts                 # Одна очередь долгов: выписка первой, дальше по ставке; лучшая ставка своих накоплений
    │   │   ├── review.ts                # Когда напомнить о пересмотре; «Скрыть» прячет одно напоминание
    │   │   ├── review-reminder.tsx      # Плашка над вкладками: пора подвести итоги квартала или года
    │   │   ├── plan-format.ts           # Рубли, проценты, месяц и дата в тексте, текст действия и совет по долгу — для экрана и PDF
    │   │   ├── plan-parts.tsx           # Карточка шага, строка «подпись — число», заметки шага
    │   │   ├── education-card.tsx       # Калькулятор образования: форма, расчёт при вводе, сохранить, сохранить как цель
    │   │   ├── pension-card.tsx         # Калькулятор пенсии: две стратегии, взнос, капитал по годам, упрощённый расчёт
    │   │   ├── pension-card.test.tsx    # Шаг «Цели» открывается, когда средние расходы в долях копейки
    │   │   ├── plan-pdf.ts              # Документ pdfmake из данных плана; pdfmake и шрифт грузятся по нажатию
    │   │   ├── plan-pdf.test.ts         # A4 и Roboto, все восемь шагов, расчёты и заметки, без узкого пробела, номера страниц
    │   │   └── steps/
    │   │       ├── diagnosis-step.tsx   # Где вы сейчас: бюджет, капитал, долги, резерв, полисы, счета
    │   │       ├── goals-step.tsx       # Цели, дети и пенсия
    │   │       ├── mechanisms-step.tsx  # Хватает ли бюджета на все цели и что делать, если нет
    │   │       ├── protection-step.tsx  # Резерв и страховка по трём группам рисков
    │   │       ├── optimization-step.tsx # Советы по расходам, долги по ставке, деньги сверх резерва
    │   │       ├── strategy-step.tsx    # Отношение к риску и классы активов по каждой цели, дисклеймер
    │   │       ├── actions-step.tsx     # Что сделать, с галочками
    │   │       └── review-step.tsx      # Даты пересмотра и отметки о нём
    │   └── not-found-page.tsx           # Неизвестный адрес
    │
    ├── components/               # ===== ОБЩИЕ КОМПОНЕНТЫ =====
    │   ├── brand/
    │   │   ├── cat-mark.ts       # Геометрия знака и значка вкладки — единственный источник для логотипа и иконок
    │   │   ├── favicon.test.ts   # Значок вкладки читается как XML, .ico из PNG, ссылки страницы
    │   │   └── cat-logo.tsx      # Логотип: рисуется currentColor, следует теме
    │   ├── ui/                   # shadcn-подобные примитивы
    │   │   ├── button.tsx        # Кнопка и варианты классов
    │   │   ├── card.tsx          # Карточка, шапка, заголовок, описание
    │   │   ├── input.tsx         # Текстовое поле
    │   │   ├── number-input.tsx  # Поле только для чисел (фильтрует ввод и вставку, вставка заменяет выделенное)
    │   │   ├── number-input.test.tsx # Вставка в выделенное и в место курсора
    │   │   ├── select.tsx        # Выпадающий список
    │   │   ├── slider.tsx        # Ползунок для «что если»
    │   │   ├── field.tsx         # Подпись, подсказка и ошибка вокруг поля
    │   │   └── confirm-dialog.tsx # Подтверждение действия (с местом под доп. поле)
    │   ├── common/
    │   │   ├── error-boundary.tsx        # Ловит ошибку экрана или виджета
    │   │   ├── notice.tsx                # Плашка над страницей: на телефоне мелкий текст, ссылка в тексте и крестик
    │   │   ├── backup-reminder.tsx       # Плашка «пора сделать копию»
    │   │   ├── data-risk-banner.tsx      # Плашка о риске потери данных: приватное окно, место, Safari, хранилище
    │   │   ├── database-blocked-dialog.tsx # Схему обновила другая вкладка — просьба перезагрузить
    │   │   └── storage-bootstrap.tsx     # Запрос постоянного хранения при первом запуске
    │   ├── theme/
    │   │   ├── theme-provider.tsx # Тёмная или светлая тема, память в localStorage, плавный переход
    │   │   ├── theme-toggle.tsx   # Переключатель: солнце и луна перетекают друг в друга
    │   │   └── theme-provider.test.tsx # Первый кадр без анимации, класс перехода снимается сам
    │   └── pwa/
    │       ├── update-prompt.tsx  # Проверка новой версии раз в час, «Вышла версия …» с заметками, «Установить»; «готово офлайн» гаснет само
    │       └── updated-notice.tsx # Один раз после обновления: что нового
    │
    ├── hooks/
    │   ├── use-settings.ts       # Настройки живым запросом, с различением «ещё читаем» и «строки нет»
    │   ├── use-data-version.ts   # Счётчик изменений из других вкладок; своей вкладки — только по просьбе
    │   ├── use-usage.ts          # Дни использования живьём; отметка дня при открытии и возвращении приложения
    │   └── use-hand.ts           # Рука для вкладок телефона живьём
    │
    ├── lib/                      # ===== УТИЛИТЫ =====
    │   ├── utils.ts              # cn() — слияние классов
    │   ├── crypto.ts             # Шифрование файла копии (AES-GCM + PBKDF2), base64 кусками по 32 КБ
    │   ├── secrets.ts            # Маскировка ключей и вычистка их из текста ошибок
    │   ├── hash.ts               # Короткий стабильный хэш (ключ записи ленты, позже — строки выписки)
    │   ├── numeric-input.ts      # Правила числового ввода (чистые функции)
    │   ├── download.ts           # Отдать файл браузеру на сохранение — копия и документы вычетов
    │   ├── statement/            # Разбор банковских выписок
    │   │   ├── decode.ts         # UTF-8 или Windows-1251 — кодировка определяется сама
    │   │   ├── values.ts         # Суммы с запятой, пробелами и знаком рубля; даты в любом виде
    │   │   ├── table.ts          # CSV через PapaParse и XLSX через ExcelJS, поиск строки заголовков
    │   │   ├── mapping.ts        # Угадывание колонок по заголовкам и по содержимому
    │   │   ├── rows.ts           # Строки в операции, отпечаток строки, дубли, правила категорий
    │   │   └── statement.test.ts # Тесты разбора, включая выписку в Windows-1251
    │   ├── feed-parser.ts        # Разбор RSS, Atom, JSON Feed и типовых ответов новостных API
    │   ├── feed-presets.ts       # Проверенные вручную источники новостей и хост для сообщения об отказе
    │   ├── persist.ts            # navigator.storage: постоянное хранение и занятое место
    │   ├── platform.ts           # Safari и iOS — для предупреждения об установке; признак приватного окна
    │   ├── data-risk.ts          # Какой риск потери данных назвать первым и когда скрытый возвращается
    │   ├── broadcast.ts          # Обмен событиями между вкладками (BroadcastChannel), метка вкладки против двойного пересчёта
    │   ├── usage.ts              # Дни использования в localStorage: первый день, дни открытия, знакомство, напоминания пилота
    │   ├── clipboard.ts          # Копирование текста: API буфера обмена, иначе скрытое поле
    │   ├── hand.ts               # Правша или левша в localStorage: вкладки телефона справа или слева
    │   ├── preview.ts            # До 9.5 выбор страны виден только с ключом konvert-me.country-choice = on
    │   ├── last-error.ts         # Последняя ошибка в памяти для письма о проблеме
    │   ├── release.ts            # Версии и заметки, «что нового» после обновления, version.json, проверка обновления раз в час
    │   └── *.test.ts             # Тесты утилит
    │
    ├── i18n/
    │   ├── index.ts              # strings: словарь языка страницы, загружается до модулей (top-level await)
    │   ├── ru.ts                 # Все тексты интерфейса на русском, одним объектом
    │   ├── en.ts                 # Все тексты на английском (Konvercat), той же формы, что ru.ts
    │   ├── types.ts              # Dictionary по форме ru.ts, формы множественного числа
    │   ├── locale.ts             # Язык страницы из localStorage и локаль для Intl
    │   ├── country.ts            # Страна данных на странице: валюта всех сумм; её ставит сеанс приложения из настроек
    │   ├── plural.ts             # pluralForm: форма слова для числа через Intl.PluralRules
    │   ├── format.ts             # percentLabel: проценты по правилам языка; inCurrency: подпись суммы со знаком валюты
    │   ├── fill.ts               # Подстановка значений в тексты словаря
    │   └── *.test.ts             # Языки, формы слов, форматы; кириллица только в словаре
    │
    └── test/
        └── setup.ts              # Тестовое окружение: jest-dom, fake-indexeddb, WebCrypto
```

---

## Статистика по модулям

| Модуль          | Файлов  | Описание                                                                                                                                                  |
| --------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| src/core/       | 43      | Финансовое ядро и тесты: формулы 1–12 раздела 5, кредитка с льготным периодом, проценты, образование, портфель, вычеты, страны, нормативы по странам      |
| src/db/         | 37      | Схема Dexie v9 со страной, стартовые категории, репозитории с проверкой Zod, копии с файлами и финпланом, миграции, счёт данных для пилота                |
| src/features/   | 113     | Экраны: лендинг и политика, знакомство, дашборд, бюджет с импортом и категориями, цели, баланс с кредиткой, вычеты, финплан, настройки, письмо о проблеме |
| src/components/ | 23      | Логотип и проверка значка, примитивы интерфейса, тема, PWA с оповещением об обновлении, общие плашки                                                      |
| src/lib/        | 35      | Шифрование, секреты, ленты, разбор выписок, числовой ввод, файлы, вкладки, дни использования, буфер обмена, рука, версии, скрытый выбор страны            |
| src/hooks/      | 4       | Настройки, версия данных, дни использования, рука                                                                                                         |
| src/app/        | 10      | Маршруты, сеанс приложения со страной данных, оболочка, вкладки по стране, настройки сайта, заметки к версиям                                             |
| src/i18n/       | 11      | Словари русского и английского, загрузка языка страницы, страна страницы, формы слов, форматы, проверка кириллицы                                         |
| src (прочее)    | 4       | main.tsx, index.css, build-info.d.ts, test/setup.ts                                                                                                       |
| e2e/            | 21      | Playwright: сайт, все вкладки и сценарии, регрессии двух проверок, язык, страна, скорость, доступность                                                    |
| public/         | 10      | Иконки приложения и вкладки, превью ссылки, снимки для лендинга                                                                                           |
| scripts/        | 3       | Отрисовка иконок и превью ссылки, публикация сайта                                                                                                        |
| docs/           | 5       | Полное ТЗ (PLAN.md), пилот (PILOT.md), глоссарий (GLOSSARY.md) и скриншоты для README                                                                     |
| root            | 20      | Конфигурации, документация, CI (.github) и запуск дев-сервера (.claude)                                                                                   |
| **Итого**       | **339** | из них 82 файла тестов                                                                                                                                    |

---

## Что дальше по плану

| Этап | Раздел                                             | Состояние           |
| ---- | -------------------------------------------------- | ------------------- |
| 0    | Каркас                                             | готово (v0.1.0)     |
| 1    | Финансовое ядро                                    | готово (v0.2.0)     |
| 2    | Хранилище, счета, копии                            | готово (v0.4.0)     |
| 3    | Знакомство и «Обзор»                               | готово (v0.6.0)     |
| 4    | «Бюджет»: операции, план-факт                      | готово (v0.9.0)     |
| 4b   | Импорт выписок (CSV, XLSX)                         | готово (v0.14.0)    |
| 5    | «Цели»: конверты, «что если»                       | готово (v0.11.0)    |
| 6    | «Баланс»: полисы, динамика капитала, лента «Скоро» | готово (v0.12.0)    |
| 7    | «Вычеты»: нормативы в `src/core/rules/<год>.ts`    | готово (v0.15.0)    |
| 8    | «Финплан»: образование, пенсия, экспорт в PDF      | готово (v0.16.0)    |
| MVP  | Сайт: лендинг, политика, статистика пилота         | готово (v0.17.0)    |
| MVP  | Публикация на GitVerse Pages                       | готово (17.09.2026) |
| MVP  | Кредитка с льготным периодом, телефон, обновления  | готово (v0.18.0)    |
| MVP  | Пилот на 10–20 людях                               | дальше              |

Требования к MVP вне этапов: скорость на 10 000 операций — проверяется
(`e2e/performance.spec.ts`, v0.16.0); доступность — тоже (`e2e/accessibility.spec.ts`, v0.16.1);
проверка на двух сценариях пройдена, находки исправлены и закреплены (`e2e/scenarios.spec.ts`, v0.16.2);
сайт для пилота готов (`e2e/site.spec.ts`, v0.17.0) и опубликован на https://konverkot.gitverse.site; проверка кредитки с льготным периодом пройдена, находки исправлены (`e2e/card-scenarios.spec.ts`, v0.18.0); пилот — впереди, по шагам docs/PILOT.md.

---

> Последнее обновление: 17.09.2026 (v0.18.0: кредитка с льготным периодом, вкладки телефона, письмо о проблеме, значок вкладки, оповещение о новой версии)
