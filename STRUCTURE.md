# Структура проекта — Конверкот

**Версия:** 0.14.0, этап 7 «Вычеты» в работе | **Дата:** 16.09.2026

---

```
konvert-me/
├── .gitignore                    # Игнорируемые файлы
├── .prettierignore               # Файлы вне форматирования (в т.ч. CLAUDE.md и docs/PLAN.md)
├── .prettierrc.json              # Настройки Prettier
├── CHANGELOG.md                  # История изменений по версиям
├── CLAUDE.md                     # Правила работы над проектом (стек, жёсткие правила, порядок работы по этапам)
├── README.md                     # Краткое описание, состояние этапов, команды
├── STACK.md                      # Стек и зависимости
├── STRUCTURE.md                  # Этот файл
├── eslint.config.js              # ESLint: общие правила + запрет зависимостей ядра от React/базы/экранов
├── index.html                    # Точка входа, мета-теги, иконки, тема до первой отрисовки
├── package.json                  # Зависимости и команды (здесь же версия приложения)
├── package-lock.json             # Lockfile
├── playwright.config.ts          # E2E: два проекта (desktop 1280px, mobile 375px), сборка + preview
├── tsconfig.json                 # Ссылки на конфигурации
├── tsconfig.app.json             # TypeScript для приложения (strict, alias @/*)
├── tsconfig.node.json            # TypeScript для конфигов и e2e
├── vite.config.ts                # Vite: React, Tailwind, PWA-манифест
├── vitest.config.ts              # Vitest: jsdom, пороги покрытия (ядро — 95 %)
│
├── docs/
│   └── PLAN.md                   # Полное ТЗ: принципы, модель данных, формулы (раздел 5 — источник истины), этапы
│
├── .claude/
│   └── launch.json               # Конфигурация запуска дев-сервера
│
├── .github/
│   └── workflows/
│       └── ci.yml                # CI: линт → типы → тесты с покрытием → сборка → e2e
│
├── scripts/
│   └── generate-icons.mjs        # Отрисовка иконок из cat-mark.ts браузером Playwright (npm run icons)
│
├── public/                       # ===== СТАТИКА =====
│   ├── favicon.svg               # Знак, меняющий цвет по системной теме
│   ├── favicon-64.png            # Растровый favicon
│   ├── apple-touch-icon.png      # Иконка для iOS
│   ├── pwa-192x192.png           # Иконка установленного приложения
│   ├── pwa-512x512.png           # Иконка установленного приложения
│   └── pwa-maskable-512x512.png  # Maskable-иконка (обрезается системой по маске)
│
├── e2e/                          # ===== E2E-ТЕСТЫ (Playwright) =====
│   ├── helpers.ts                # skipOnboarding() — общий пролог для тестов не про знакомство
│   ├── navigation.spec.ts        # Маршруты, панель вкладок сверху, тема, отсутствие горизонтальной прокрутки на 375 px
│   ├── pwa.spec.ts               # Манифест и работа без интернета (реальный service worker)
│   ├── storage.spec.ts           # Счета, источники дохода, числовые поля, копия → очистка → импорт, две вкладки
│   ├── onboarding.spec.ts        # Пять шагов ≤ 25 действий, дашборд, добавление и перестановка виджетов, быстрый ввод
│   ├── sources.spec.ts           # Закладки, новости, ключи API, отказ источника
│   ├── budget.spec.ts            # Операции всех видов, план-факт, копирование плана, поиск и фильтры, год
│   ├── goals.spec.ts             # Векторы плана через интерфейс, взнос и конверты, «что если», распределение
│   ├── balance.spec.ts           # Капитал и кредитная нагрузка, график, лимит вкладов, полисы, «Скоро»
│   ├── import.spec.ts            # Выписка в Windows-1251, повторный импорт, правила, дубли, отмена
│   └── deductions.spec.ts        # Примеры ФНС на экране (обучение, дети, продажа), жильё, чек-лист, годы, документы, удаление
│
└── src/                          # ===== ИСХОДНЫЙ КОД =====
    ├── main.tsx                  # Точка входа React (ThemeProvider + BrowserRouter)
    ├── index.css                 # Tailwind, чёрно-белая палитра в CSS-переменных, плавный переход темы
    │
    ├── app/                      # Оболочка приложения
    │   ├── App.tsx               # Маршруты: /welcome отдельно, остальные внутри оболочки
    │   ├── app-shell.tsx         # Панель вкладок над страницей, напоминание о копии, Error Boundary
    │   └── navigation.ts         # Список вкладок: адрес, подпись, значок
    │
    ├── core/                     # ===== ФИНАНСОВОЕ ЯДРО =====
    │   │                         # Чистые функции: без React, без базы, без экранов (правило проверяется ESLint)
    │   ├── money.ts              # Копейки: проверки, округление half-up, формат ru-RU, вывод прогноза
    │   ├── time.ts               # Даты строками ГГГГ-ММ-ДД и месяцы ГГГГ-ММ, разница месяцев, без часовых поясов
    │   ├── goals.ts              # Формулы 2-6 и 8: будущая стоимость, взнос, срок перебором 1..1200, реальная доходность, распределение остатка
    │   ├── budget.ts             # Формула 7: доходы минус расходы, агрегаты месяца и года, план против факта
    │   ├── balance.ts            # Формулы 9-11 + динамика капитала по месяцам и вклады по банкам
    │   ├── payday.ts             # Ближайшая выплата по числу месяца и сколько до неё дней
    │   ├── upcoming.ts           # Лента «Скоро»: платежи, вклады, полисы, льготный период
    │   ├── deductions.ts         # НДФЛ по шкале, возврат, вычеты: дети, социальный, ИИС, жильё с остатком; продажа жилья и итог года
    │   ├── rules/                # Нормативы по годам: значение, источник (URL), дата проверки
    │   │   ├── types.ts          # Norm<T>, ступень шкалы НДФЛ, детские вычеты, продажа жилья, набор правил года
    │   │   ├── sources.ts        # Адреса страниц нормативов (одна страница — на несколько лет), в т. ч. статьи 210, 228, 229 НК
    │   │   ├── 2023.ts           # Социальный лимит 120 000, обучение ребёнка 50 000, НДФЛ 13/15 %, ИИС типа А, продажа по 13 %
    │   │   ├── 2024.ts           # Социальный лимит 150 000, обучение ребёнка 110 000, ПДС и ИИС-3 — общий лимит 400 000
    │   │   ├── 2025.ts           # Пять ступеней НДФЛ, детские 1 400 / 2 800 / 6 000, продажа — отдельная база 13/15 %
    │   │   ├── 2026.ts           # То же + лимит страхования вкладов 1 400 000 ₽ и оговорка про ст. 219.2 с 01.09.2026
    │   │   └── index.ts          # rulesForYear: свой год или последний записанный; раньше первого — ничего
    │   ├── types.ts              # Входные типы ядра (операция, счёт, категория, план)
    │   ├── index.ts              # Реэкспорт
    │   └── *.test.ts             # Векторы раздела 5 плана; для вычетов — примеры ФНС и правила закона, у каждого вектора подписан источник
    │
    ├── db/                       # ===== ХРАНИЛИЩЕ =====
    │   ├── db.ts                 # Dexie: таблицы, схема v1 → v5 (ленты, источники дохода, полисы, вычеты и документы), versionchange
    │   ├── models.ts             # Zod-схемы всех сущностей, значения по умолчанию, версия схемы
    │   ├── validate.ts           # parseOrThrow: ошибки Zod → одно понятное сообщение
    │   ├── errors.ts             # RepositoryError и ValidationError
    │   ├── backup.ts             # Экспорт/импорт всех таблиц с файлами документов в base64, шифрование, очистка базы
    │   ├── migration.test.ts     # Миграции v1 → v2 и v4 → v5 (на настоящей цепочке версий), обновление схемы из другой вкладки
    │   └── repositories/         # Единственная дверь к данным для интерфейса
    │       ├── accounts.ts       # Счета и долги, расчётный остаток, инвариант конвертов
    │       ├── transactions.ts   # Операции: правка, удаление, фильтры, поиск, пакетный импорт и его отмена
    │       ├── categories.ts     # 21 стартовая категория из шаблона курса
    │       ├── budget-plans.ts   # План месяца и года по категориям, копирование с прошлого месяца
    │       ├── income-sources.ts # Источники дохода и отсчёт до ближайшей выплаты
    │       ├── policies.ts       # Страховые полисы: срок, сумма, страховая компания
    │       ├── deductions.ts     # Год вычетов (ключ — год): расходы, дети, продажа, собранные бумаги; удаление с документами
    │       ├── documents.ts      # Документы: описание отдельно от байтов, лимит 10 МБ, файл по требованию
    │       ├── goals.ts          # Цели, приоритеты, цель «Резерв», конверты
    │       ├── dashboard.ts      # Раскладка «Обзора»: набор виджетов, ширина, высота, порядок
    │       ├── settings.ts       # Настройки, дата последней копии, напоминание раз в 30 дней
    │       ├── links.ts          # Закладки (только https, никаких запросов)
    │       ├── feeds.ts          # Ленты новостей: ключи API, запрос, разбор, ошибки без секретов
    │       └── *.test.ts         # Тесты репозиториев на fake-indexeddb
    │
    ├── features/                 # ===== ЭКРАНЫ =====
    │   ├── onboarding/
    │   │   ├── onboarding-page.tsx      # Пять шагов, прогресс, «Пропустить»
    │   │   ├── complete-onboarding.ts   # Ответы → категории, план, счета, цели, раскладка
    │   │   └── onboarding.test.ts       # Проверка результата знакомства и первых цифр «Обзора»
    │   ├── overview/
    │   │   ├── overview-page.tsx        # Дашборд: режим настройки, каталог, сброс
    │   │   ├── overview-data.ts         # Один расчёт на весь экран + список предупреждений
    │   │   ├── dashboard-grid.tsx       # Сетка 4/2/1 колонки, dnd-kit, Error Boundary на каждый виджет
    │   │   ├── widget-catalog.tsx       # Диалог «Добавить виджет»
    │   │   └── widgets/
    │   │       ├── registry.ts          # Реестр: тип, название, описание, размеры, компонент
    │   │       ├── types.ts             # Контракт виджета
    │   │       ├── widget-shell.tsx     # Рамка, пустое состояние, прогресс-бар, крупное число
    │   │       ├── basic-widgets.tsx    # Свободный остаток, резерв, капитал, кредитная нагрузка
    │   │       ├── goal-widgets.tsx     # Все цели, прогресс выбранной цели, предупреждения
    │   │       ├── quick-add-widget.tsx # Быстрый ввод операции
    │   │       └── source-widgets.tsx   # Полезные источники и новости
    │   ├── balance/
    │   │   ├── balance-page.tsx         # Счета и долги, капитал, график, страхование вкладов, архив
    │   │   ├── balance-data.ts          # Один расчёт на экран: остатки, ряд капитала, банки, полисы
    │   │   ├── capital-chart.tsx        # График капитала по месяцам (Recharts, отдельный кусок)
    │   │   ├── policies-card.tsx        # Страховые полисы с отсчётом до окончания
    │   │   ├── payday-card.tsx          # Источники дохода и отсчёт до ближайшей выплаты
    │   │   ├── days-label.ts            # Склонение «день / дня / дней»
    │   │   └── account-form.tsx         # Диалог счёта: тип, остаток, банк, ставка, даты, платёж
    │   ├── settings/
    │   │   ├── settings-page.tsx        # Сборка карточек настроек
    │   │   ├── backup-actions.ts        # Скачивание копии, импорт файла, полная очистка
    │   │   └── cards/
    │   │       ├── calculation-card.tsx # Инфляция, доходность, размер резерва
    │   │       ├── backup-card.tsx      # Копия с паролем, импорт с подтверждением, удаление данных
    │   │       ├── storage-card.tsx     # Постоянное хранение и занятое место
    │   │       ├── install-card.tsx     # Установка приложения и предупреждение про Safari
    │   │       ├── sources-card.tsx     # Выключатель внешних источников, список лент, проверка после сохранения
    │   │       └── feed-form.tsx        # Адрес ленты, способ доступа, ключ, путь к записям
    │   ├── budget/
    │   │   ├── import/
    │   │   │   ├── import-card.tsx     # Кнопка импорта и список импортов с отменой
    │   │   │   └── import-wizard.tsx   # Мастер: файл и счёт → колонки → проверка (свой чанк)
    │   │   ├── budget-page.tsx         # Вкладки «Месяц» и «Год», переключатель периода, диалоги
    │   │   ├── budget-data.ts          # Один расчёт на экран: строки план-факт, итоги месяца и года
    │   │   ├── month-label.ts          # «2026-09» → «Сентябрь 2026» и «сен», без часовых поясов
    │   │   ├── plan-fact-table.tsx     # План / факт / отклонение по категориям, план правится в строке
    │   │   ├── year-table.tsx          # Двенадцать месяцев и итог, под фактом серым — план
    │   │   ├── transactions-card.tsx   # Список операций месяца, поиск и фильтры
    │   │   ├── transaction-filter.ts   # Состояние фильтров списка
    │   │   └── transaction-form.tsx    # Диалог операции: поля меняются под вид
    │   ├── goals/
    │   │   ├── goals-page.tsx           # Список целей с перетаскиванием, распределение свободных денег
    │   │   ├── goals-data.ts            # Один расчёт на экран: взносы, резерв, распределение
    │   │   ├── goal-form.tsx            # Диалог цели: стоимость, срок, доходность, инфляция
    │   │   ├── goal-details.tsx         # Карточка цели: цифры, график, конверты, взнос, «что если»
    │   │   ├── goal-chart.tsx           # График накоплений против цены (Recharts, отдельный кусок)
    │   │   ├── envelopes-panel.tsx      # Конверты по счетам и форма взноса
    │   │   ├── what-if-panel.tsx        # Ползунки срока, стоимости и доходности
    │   │   └── months-label.ts          # Склонение «месяц / месяца / месяцев»
    │   ├── deductions/
    │   │   ├── deductions-page.tsx      # Годы вкладками (идёт / открыт / срок прошёл), анкета и документы выбранного года
    │   │   ├── deductions-data.ts       # Один расчёт на экран: какие годы показать, правила года, итог, документы
    │   │   ├── deductions-data.test.ts  # Какие годы видны, прошедший год без правил, свои правила у каждого года
    │   │   ├── year-panel.tsx           # Анкета «что мне положено»: дети, расходы, жильё, продажа; итог пересчитывается при вводе
    │   │   ├── refund-card.tsx          # Итог года: части возврата, налог с продажи, сроки декларации (и если они прошли)
    │   │   ├── refund-card.test.tsx     # Сроки до 30 апреля, между 30 апреля и 15 июля, после; продажа без налога
    │   │   ├── checklist.ts             # Какие бумаги нужны по ответам анкеты и году (урок 3.7; с 2024 — справки об оплате)
    │   │   ├── checklist.test.ts        # Пустой список, справка против договора и лицензии, дети у работодателя, ипотека, продажа
    │   │   ├── checklist-card.tsx       # Чек-лист под анкетой: группы, галочки, «собрано N из M», файлы каждого вида
    │   │   ├── fill.ts                  # Подстановка значений в тексты ru.ts
    │   │   └── documents-card.tsx       # Загрузка по видам до 10 МБ, открытие, удаление, занятое место
    │   ├── plan/plan-page.tsx           # Заглушка (этап 8)
    │   └── not-found-page.tsx           # Неизвестный адрес
    │
    ├── components/               # ===== ОБЩИЕ КОМПОНЕНТЫ =====
    │   ├── brand/
    │   │   ├── cat-mark.ts       # Геометрия знака — единственный источник для логотипа и иконок
    │   │   └── cat-logo.tsx      # Логотип: рисуется currentColor, следует теме
    │   ├── ui/                   # shadcn-подобные примитивы
    │   │   ├── button.tsx        # Кнопка и варианты классов
    │   │   ├── card.tsx          # Карточка, шапка, заголовок, описание
    │   │   ├── input.tsx         # Текстовое поле
    │   │   ├── number-input.tsx  # Поле только для чисел (фильтрует ввод и вставку)
    │   │   ├── select.tsx        # Выпадающий список
    │   │   ├── slider.tsx        # Ползунок для «что если»
    │   │   ├── field.tsx         # Подпись, подсказка и ошибка вокруг поля
    │   │   └── confirm-dialog.tsx # Подтверждение действия (с местом под доп. поле)
    │   ├── common/
    │   │   ├── error-boundary.tsx        # Ловит ошибку экрана или виджета
    │   │   ├── backup-reminder.tsx       # Плашка «пора сделать копию»
    │   │   ├── database-blocked-dialog.tsx # Схему обновила другая вкладка — просьба перезагрузить
    │   │   ├── storage-bootstrap.tsx     # Запрос постоянного хранения при первом запуске
    │   │   └── stub-page.tsx             # Оформление вкладок-заготовок
    │   ├── theme/
    │   │   ├── theme-provider.tsx # Тёмная или светлая тема, память в localStorage, плавный переход
    │   │   ├── theme-toggle.tsx   # Переключатель: солнце и луна перетекают друг в друга
    │   │   └── theme-provider.test.tsx # Первый кадр без анимации, класс перехода снимается сам
    │   └── pwa/
    │       └── update-prompt.tsx  # Предложение обновиться; «готово офлайн» гаснет само
    │
    ├── hooks/
    │   ├── use-settings.ts       # Настройки живым запросом, с различением «ещё читаем» и «строки нет»
    │   └── use-data-version.ts   # Счётчик изменений из других вкладок для перезапроса данных
    │
    ├── lib/                      # ===== УТИЛИТЫ =====
    │   ├── utils.ts              # cn() — слияние классов
    │   ├── crypto.ts             # Шифрование файла копии (AES-GCM + PBKDF2), base64 кусками по 32 КБ
    │   ├── secrets.ts            # Маскировка ключей и вычистка их из текста ошибок
    │   ├── hash.ts               # Короткий стабильный хэш (ключ записи ленты, позже — строки выписки)
    │   ├── numeric-input.ts      # Правила числового ввода (чистые функции)
    │   ├── download.ts           # Отдать файл браузеру на сохранение — копия и документы вычетов
    │   ├── statement/            # Разбор банковских выписок
    │   │   ├── decode.ts         # UTF-8 или Windows-1251 — кодировка определяется сама
    │   │   ├── values.ts         # Суммы с запятой, пробелами и знаком рубля; даты в любом виде
    │   │   ├── table.ts          # CSV через PapaParse и XLSX через ExcelJS, поиск строки заголовков
    │   │   ├── mapping.ts        # Угадывание колонок по заголовкам и по содержимому
    │   │   ├── rows.ts           # Строки в операции, отпечаток строки, дубли, правила категорий
    │   │   └── statement.test.ts # Тесты разбора, включая выписку в Windows-1251
    │   ├── feed-parser.ts        # Разбор RSS, Atom, JSON Feed и типовых ответов новостных API
    │   ├── feed-presets.ts       # Проверенные вручную источники новостей и хост для сообщения об отказе
    │   ├── persist.ts            # navigator.storage: постоянное хранение и занятое место
    │   ├── platform.ts           # Safari и iOS — для предупреждения об установке
    │   ├── broadcast.ts          # Обмен событиями между вкладками (BroadcastChannel)
    │   └── *.test.ts             # Тесты утилит
    │
    ├── i18n/
    │   └── ru.ts                 # Все тексты интерфейса на русском, одним объектом
    │
    └── test/
        └── setup.ts              # Тестовое окружение: jest-dom, fake-indexeddb, WebCrypto
```

---

## Статистика по модулям

| Модуль          | Файлов  | Описание                                                                        |
| --------------- | ------- | ------------------------------------------------------------------------------- |
| src/core/       | 27      | Финансовое ядро и тесты: формулы 1-11 раздела 5, вычеты, нормативы 2023–2026    |
| src/db/         | 30      | Схема Dexie v5, репозитории с проверкой Zod, копии с файлами, миграции          |
| src/features/   | 61      | Экраны: знакомство, дашборд, бюджет с импортом, цели, баланс, вычеты, настройки |
| src/components/ | 19      | Логотип, примитивы интерфейса, тема, PWA, общие плашки                          |
| src/lib/        | 23      | Шифрование, секреты, ленты, разбор выписок, числовой ввод, скачивание файлов    |
| src/hooks/      | 2       | Настройки и версия данных                                                       |
| src/app/        | 3       | Маршруты и оболочка                                                             |
| src/i18n/       | 1       | Тексты интерфейса                                                               |
| src (прочее)    | 3       | main.tsx, index.css, test/setup.ts                                              |
| e2e/            | 11      | Playwright: навигация, PWA, хранилище, бюджет, импорт, цели, баланс, вычеты     |
| public/         | 6       | Иконки приложения                                                               |
| scripts/        | 1       | Отрисовка иконок                                                                |
| docs/           | 1       | Полное ТЗ (PLAN.md)                                                             |
| root            | 20      | Конфигурации, документация, CI (.github) и запуск дев-сервера (.claude)         |
| **Итого**       | **208** | из них 44 файла тестов                                                          |

---

## Что дальше по плану

| Этап | Раздел                                             | Состояние        |
| ---- | -------------------------------------------------- | ---------------- |
| 0    | Каркас                                             | готово (v0.1.0)  |
| 1    | Финансовое ядро                                    | готово (v0.2.0)  |
| 2    | Хранилище, счета, копии                            | готово (v0.4.0)  |
| 3    | Знакомство и «Обзор»                               | готово (v0.6.0)  |
| 4    | «Бюджет»: операции, план-факт                      | готово (v0.9.0)  |
| 4b   | Импорт выписок (CSV, XLSX)                         | готово (v0.14.0) |
| 5    | «Цели»: конверты, «что если»                       | готово (v0.11.0) |
| 6    | «Баланс»: полисы, динамика капитала, лента «Скоро» | готово (v0.12.0) |
| 7    | «Вычеты»: нормативы в `src/core/rules/<год>.ts`    | в работе         |
| 8    | «Финплан»: образование, пенсия, экспорт в PDF      | не начат         |

---

### Этап 7 «Вычеты»: что сделано и что осталось

| Пункт плана                                                | Состояние                                                                |
| ---------------------------------------------------------- | ------------------------------------------------------------------------ |
| Нормативы по годам со ссылкой на источник и датой проверки | готово: 2023–2026, тест не пропускает норматив без источника             |
| Анкета «что мне положено»                                  | готово: лечение, обучение, спорт, ДМС, дети, дорогое лечение, ИИС, жильё |
| Сумма к возврату с лимитами, ставкой НДФЛ, сроком 3 года   | готово: сходятся примеры ФНС и опубликованные примеры по шкале           |
| Хранение чеков и договоров, лимит, индикатор места         | готово: схема v5, 10 МБ на файл, файлы попадают в резервную копию        |
| Чек-лист документов (урок 3.7)                             | готово: по ответам анкеты и году, галочки хранятся с годом               |
| Напоминание в январе                                       | не начат                                                                 |
| Виджет «Вычеты: можно вернуть» на «Обзоре»                 | не начат                                                                 |
| Предупреждения о риске потери данных (просьба владельца)   | не начат                                                                 |
| Вычет на детей (стандартный), продажа жилья                | готово: пример ФНС на детей, примеры ФНС и урока 3.5 на продажу          |

---

> Последнее обновление: 16.09.2026 (этап 7 «Вычеты» в работе: ядро,
> хранилище v5, экран; не выпущен)
