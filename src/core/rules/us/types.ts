import type { YearRules } from '../types';

/**
 * The norms of a year in the United States. So far only the common ones: the limits of the tax
 * advantaged accounts join them with the economy of the country.
 */
export type UsYearRules = YearRules;
